# BE 工作日志 — 2026-07-09

## 10:53

小吴选型结论我先接了。核心问题直接回答：  ---  **一个 WebSocket 就够了，不需要两个。**  腾讯云的「实时说话人分离」是独立 API，架构上是实时语音识别 + 声纹聚类合二为一。服务端只需要维护一条 `wss://` 连接：  ``` 手机麦克风 → PCM 16kHz 音频流 → 一个 WebSocket → 返回带 speaker_id 的识别结果 ```  返回 JSON 大概长这样：  ```json {   "code": 0,   "message": "success",   "voice_id": "xxx",   "result": {     "slic

## 10:58

查完了，跟 boss 汇报一下核查结果：  ---  ## 🔍 事实核查：之前的选型结论有哪里需要修正  ### ✅ 腾讯云 — 结论正确，无需修正  确认有独立的「实时说话人分离 WebSocket」API（文档号 1093/131127），一个 `wss://` 连接同时返回识别文本 + `speaker_id`（0/1）。2026 年 5 月正式上线，文档里写得明明白白。`voice_id` 每连接必换 UUID，断了就作废——我之前说的重连机制也站得住。  ### ⚠️ 讯飞 — 之前说的"仅录音文件"不够准确，需要修正  讯飞有「实时语音转写大模型」，也支持**实时角色分离**，不是

## 11:09

收到，小柯。PoC 阶段后端确实没活——我先记下两件事：  1. **未来对接点**：你那边 PCM 流从原生推到 JS 后，是前端直接开 WebSocket 传给我，还是你走一轮 JS 再转发？我倾向原生层直接怼 WebSocket，少一层序列化开销。不过这是后面的事，你先跑通 PoC。  2. **ASR 侧我准备好了**：腾讯云实时说话人分离 API 的握手协议和 PCM 格式要求（16kHz / 16bit / 单声道 / 1280 字节每包）我已经摸清了，你那边输出格式对齐就行。  boss，后端这边暂时挂机，等小柯 PoC 跑通了我再进场——正好趁这会儿把 FastAPI 项目骨架

