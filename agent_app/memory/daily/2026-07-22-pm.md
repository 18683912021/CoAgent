# PM 工作日志 — 2026-07-22

## 14:15

好的 boss，三份文档啃完了，工作空间也扫了一圈，汇报如下：  ---  ## 📋 AI Interview Copilot — 当前状态  **我们在做什么：** 一个纯 App 的 AI 面试外挂。用户手机同时跑视频面试和 Copilot，系统音频回采面试官声音 → ASR 转文字 → DeepSeek 秒出答案。四大武器：实时识别、AI 秒回答、私人知识库、AI 面试情报。  **阶段零进度：**  | 阶段 | 内容 | 状态 | |------|------|:--:| | 0.1 | 技术选型 | ✅ | | 0.2 | 系统音频双轨采集 | ✅ | | 0.3 | ASR 转文

## 14:16

进度完全对得上，boss。  我刚才翻过 `tasks.md` 和 `STATUS.md`，结论一致：0.1 → 0.2 → 0.3 三关绿灯，0.4 是下一个突破口，跟小柯说的没差。  不过我注意到一个事儿：**`workspace/shared/tasks/` 下只有模板，0.1~0.3 的任务文档都没建。** 之前阶段已经跑完了，现在补意义不大。但 0.4 马上要开工，我建议在 boss 喊"启动"之前把 0.4 的任务文档先出了——PRD 级别的任务描述 + FE/BE 分拆 + 验收标准，省得三方各脑补各的。  boss 怎么说，要我现在出 0.4 的任务文档不？

## 14:21

好的 boss，我已经把三份产品文档和工作空间都扫完了。下面是 0.4 的规划——  ---  ## 📐 0.4 产品设计思路  **核心链路**：ASR 识别到面试官说完一句话（system 轨 is_final）→ 后端自动调 DeepSeek → 流式推送回答 → 前端打字机展示  **跟 0.3 的区别**：0.3 只是把识别文字 dump 到屏幕上验证"能转对"。0.4 要上真正的对话卡片界面——面试官问题 + AI 回答成对展示，流式打字机效果。  **接口策略**：复用现有 WebSocket `/ws/audio/stream`（8010），扩展消息协议加 LLM 通道。连接时

## 14:23

任务文档已出，现在派活——  ---  ## 📋 0.4 LLM 回答生成 — 派发  @小柯（前端） @AI酱瓜（后端开发工程师） 接活了，下面是你们各自的口袋：  ### 🔵 小柯 — 0.4.1-FE：对话卡片界面 + 流式回答展示  扔掉测试界面那六个可视化组件，上真正的面试对话 UI：  - 对话气泡流（面试官灰色左对齐 / AI 蓝色右对齐） - 打字机流式效果（`llm_start` → 创建气泡 + 加载动画 → `llm_chunk` 逐字追加 → `llm_done` 收尾） - 四态覆盖：正常 / 连续多问不覆盖 / LLM 失败红色提示 / 空态"等待面试官提问..."

## 14:23

@小柯 草案看了，三个点：  **1. 方案整体 OK。** 单消息类型 `llm_answer` + `chunk_index=0` 起新气泡、`is_final` 收尾——语义清晰，比我文档里三类型的方案更简洁，就用你的。  **2. 补一个 `config` 帧。** 你草案里没写，但我文档里留了。连接建立后前端发一帧告诉后端 LLM 开不开、用哪个模型、max_tokens 多少：  ```json {"type": "config", "llm": {"enabled": true, "model": "deepseek-chat", "max_tokens": 300}} ```

## 14:25

文档已锁定，最终契约敲定 👇  ---  ## 🚀 0.4 LLM 回答生成 — 正式派发  @小柯（前端） @AI酱瓜（后端开发工程师） 接活，契约最终版：  ### 🔌 一句话版契约  **单消息类型 `llm_answer`，不复用三类型。** 小柯的草案胜出——`chunk_index=0` 起新气泡、`is_final=true` 收尾，比三类型简洁。`config` 帧保留。  ### 🟢 酱瓜特别注意  1. 最终契约是单类型 `llm_answer`（不是你之前确认的三类型），别写岔了 2. `question_text` 只在 `chunk_index=0` 时带，后面 ch

## 14:25

第三个点我拍板：**config 帧保留。**  理由很简单——现在不加以后加就是改协议，趁现在通道还是热的，一帧的事。0.4 阶段收到 config 就默认 `llm.enabled=true`，酱瓜不用额外做开关逻辑，但协议位先占好。  ---  ## ✅ 最终契约确认  | 项 | 决定 | |----|------| | LLM 消息类型 | 单 `llm_answer`（`chunk_index` / `is_final` / `question_text`） | | 触发 | `source=="system" && is_final==true` | | config 帧 | *

## 14:26

认。config 帧砍掉，0.4 不搞过度设计。  理由小柯说得对——面试场景 LLM 开着是唯一状态，不存在"不启用"。多一帧就是多一次无意义的握手延迟。  ---  ## ✅ 最终契约（定稿，不再变）  ```json {"type":"llm_answer","chunk":"...","chunk_index":0,"is_final":false,"question_text":"请介绍一下你的项目经验"} ```  | 项 | 决定 | |----|------| | 消息类型 | 单 `llm_answer` | | config 帧 | **不加**，LLM 默认开启，max_

## 14:26

文档已更新，config 帧已砍。最终版就这一条消息，干净利落 👇  ```json {"type":"llm_answer","chunk":"...","chunk_index":0,"is_final":false,"question_text":"..."} ```  | 项 | 拍板 | |----|------| | 消息类型 | 单 `llm_answer` ✅ | | config 帧 | **不加** ✅ | | max_tokens | 后端写死 300 ✅ | | 触发 | system + is_final ✅ | | 串行队列 | 酱瓜消化 ✅ |  ---  @AI

## 14:29

任务文档和 STATUS 已更新。酱瓜 0.4.2 + 0.4.4 ✅  有个小对齐：最终契约是砍了 config 帧的，但酱瓜顺手实现了——我觉得没问题，**只要不发 config 帧 LLM 也默认开启就行**，config 变成可选的增强开关。酱瓜确认一下这个行为？  ---  **当前局面：**  | 任务 | 负责人 | 状态 | |------|--------|:--:| | 0.4.2 System Prompt + DeepSeek | 酱瓜 | ✅ | | 0.4.4 说话人过滤 | 酱瓜 | ✅ | | 0.4.1 对话卡片界面 | 小柯 | ⬜ | | 0.4.3 端到

## 14:33

方向完全对。三个关键点：  **断句逻辑**：3 秒阈值合理。后端 ASR 每段 transcription 带时间戳，相邻两段 `source=="mic"` 且间隔 >3 秒 → 前端渲染分割线，新对话段。但要注意 ASR 本身有停顿（面试官说话中间的 1-2 秒停顿不该断），建议用**连续静音 3 秒**而非任意间隔，避免一句话被拆开。  **聊天展示**：面试官 (mic) 左气泡灰底，用户/你 (system) 右气泡绿底，跟微信一样。每个气泡旁边带时间戳，分割线显示「3s 静音间隔」。  **手动选择查询**：长按或点击面试官的气泡 → 底部弹出「查询回答建议」→ 把该气泡 + 前

## 14:35

状态更新完毕。0.4 全员代码完成，进入联调 🔄  ---  @AI小柯（前端） @AI酱瓜（后端开发工程师） 两位，联调走起：  - **酱瓜启动**：`cd poc-audio-capture && uvicorn app.main:app --reload --port 8000`（确保 `.env` 有 `DEEPSEEK_API_KEY`） - **小柯连**：`ws://localhost:8000/api/ws/audio/stream`，发 `session_start` → 开始推 PCM  10 道题走通，首字延迟 < 2s，群里吼我验收 🎯  ---  另外，你之前提的「

## 14:36

拍板：**方案 B，FE 改。**  两个问题一句话：契约在 0.4 定稿时已锁定——单 `llm_answer`、砍 config。FE 没对齐就 FE 改，没有「BE 迁就 FE」的道理。  理由不展开了，酱瓜说得够清楚：一个消息类型承载完整生命周期，不需要状态机。  ---  @AI小柯（前端） Kotlin 侧改一个 case，成本很低：  ``` // 删掉 llm_start / llm_chunk / llm_done 三态解析 // 改为单一 llm_answer，chunk_index==0 起泡，is_final 收尾 ```  config 帧的发包逻辑也一并删掉，别发无

## 14:37

方向合理，但有几个建议你先听一下再决定。  ---  **整体判断：比自动触发方案更好。**  自动检测"面试官说完"本身就有判断不准的问题（万一他停顿 2 秒还没说完？）。你改手动点击，是把触发权还给用户——用户自己判断「这个问题我需要帮助」的时刻，比算法准。  ---  **四个建议：**  1. **上下文别只传一个气泡。** 用户点面试官气泡，至少把前面 1-2 轮对话一起带给 LLM，不然 AI 不知道聊到哪了。比如面试官问"那你在里面负责什么"，单独这句没上下文 LLM 会懵——加上前面"我做过 XX 项目"就通了。  2. **回答模式用 System Prompt 控制，不用动

## 14:44

方向全部确认，比我想的简洁：  ---  **逐一确认：**  | 你的决定 | 我的判断 | |----------|:--:| | 精简 200 / 普通 500 / 详细深入 | ✅ 合理，三个档次拉开差距 | | 用户气泡也能点 | ✅ 务实，"假装重复"是个好场景 | | 不传上下文，单气泡 | ✅ 纯手动模式下每个问题独立，够用 | | 不管遮挡面试官 | ✅ 手机场景，面试官只看你盯屏幕 |  ---  **System Prompt 方向认可。** P8 角色 + 先判断语音识别文字质量再回答——这个很对，ASR 转出来的可能有错别字（比如"Java"识别成"加班"），让 LL

## 14:50

PRD 出完了，协议精简到两条消息：  ---  **核心变化一句话：** 砍自动触发，改手动点气泡 → 选模式 → 生成。  | 0.4（旧） | 0.5（新） | |-----------|-----------| | 面试官说完自动触发 | 点哪个气泡生成哪个 | | 一种回答长度 | 精简 ≤200 / 普通 ≤500 / 详细 800 | | `question_text` 回传 | FE 已知，删掉 | | 只面试官气泡可触发 | 面试官 + 用户气泡都能点 |  **协议：FE → BE 发 `llm_query`，BE → FE 回 `llm_answer`，其余不动。**

## 14:52

派发完成，开工。  ---  **@AI小柯（前端）** 0.5 任务：`workspace/shared/tasks/0.5-manual-trigger-chat-ui.md`  - **0.5.1-FE**：对话气泡界面（面试官左灰 / 用户右绿 / AI 左蓝虚线，3s 断句分割线） - **0.5.2-FE**：点击气泡 → 底部弹模式选择 → 发 `llm_query` → 打字机渲染 AI 气泡 - **0.5.3-FE**：底部 Sheet 三按钮面板 - **0.5.4-FE**：清理 0.4 自动触发预期，防御非预期 `llm_answer`  ---  **@AI酱瓜（后

## 14:56

更新完了，直接派发。  ---  **@AI小柯（前端）** 增量：  - **0.5.7-FE**：底部面板增加语言选择——「中文」「English」两个按钮，默认中文。`llm_query` 加 `language` 字段（`"zh"` / `"en"`）  **@AI酱瓜（后端开发工程师）** 增量：  - **0.5.8-BE**：`llm_query` 接收 `language` 字段，透传进 System Prompt。System Prompt 追加两句：「本产品仅用于程序员技术面试」「回答语言：中文/英文」  ---  协议新增一个字段，零破坏：  ```json {"type

