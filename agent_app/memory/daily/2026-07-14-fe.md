# FE 工作日志 — 2026-07-14

## 09:03

**最佳方案：Expo Dev Client，完全免费，和 Expo Go 体验一样。**  你现在缺的是 `android/` 目录（还没 prebuild），两步搞定：  ### 1. 生成原生项目 + 构建 dev APK  ```bash # 生成 android/ 和 ios/ 目录（用 Android Studio 打开 android/） npx expo prebuild  # 安装 dev-client，项目其实已经有 expo-updates，加一个包即可 npx expo install expo-dev-client ```  然后用 Android Studio 打开

## 09:05

`android/` 有了，`expo-dev-client` 也已经在 `package.json` 里了 ✅  **答案：完全可以，本地热更跟 Expo 账号一毛钱关系没有。**  你现在这套体系：  ``` ┌─────────────────────────────────────────────────┐ │  Android Studio 打 debug APK（装手机上）          │ │        ↓                                        │ │  打开 APK → 输入 exp://你的IP:8081

## 09:07

搞定 ✅。清掉了三样东西：  | 清掉的 | 说明 | |--------|------| | `updates` 块 | OTA 远程热更新（消耗 EAS Update 额度） | | `runtimeVersion` | OTA 配套的版本锁定 | | `extra.eas` | EAS 项目 ID |  —— 回答你：**完全不影响。** 两张图说清楚：  ``` 🔴 你关掉的：OTA 远程更新    APK 已发布到线上 → 不发版直接推送新 JS bundle → 走 EAS 服务器 → 消耗额度  🟢 你正在用的：本地热更（Fast Refresh）    电脑 Metro bun

## 09:59

TypeScript 编译通过 ✅ 零错误。迁移完成。  ---  ## 迁移总结：Expo → React Native CLI  ### 改了什么  | 层 | 动作 | |----|------| | **入口** | `expo-router/entry` → `index.js` + `src/App.tsx` | | **路由** | `expo-router` 文件路由 → `@react-navigation/native` v6 | | **依赖** | 砍掉 12 个 expo-* 包，保留 1 个 | | **Babel** | `babel-preset-expo` →

## 10:02

说实话，**不需要重新 `init`**。原因是：  ---  ### 当前状态其实很干净了  ``` ✅ index.js              → 标准 RN CLI 入口（AppRegistry.registerComponent） ✅ package.json          → 无 expo-* 依赖（除了 expo-modules-core 原生桥接） ✅ metro.config.js       → @react-native/metro-config（标准 CLI 配置） ✅ babel.config.js       → @react-native/babel-pre

## 10:06

一张图说清楚：  ``` npx react-native start   └─ 启动 Metro 打包器（开发服务器）      JS/TS 代码 → 实时编译成 bundle → 热更推给手机      相当于 "npx expo start" 的裸 RN 版本  npx react-native run-android   └─ 编译 Android 原生代码（Gradle）→ 装 APK 到设备 → 启动 App      相当于 "expo run:android" 的裸 RN 版本 ```  ### 会打开电脑的虚拟安卓吗？  **不会。** `run-android` 不会帮你启

## 10:14

**不需要。** `android/` 目录只在初始化时生成一次，后续它就是你的项目源码目录——跟 `src/` 一样，直接改、直接用。  ---  ### 改代码 → 对应的操作  | 你改了什么 | 需要做什么 | `android/` 动不动 | |------------|-----------|-------------------| | JS/TS 代码（`src/`） | 什么都不做，Fast Refresh 秒级热更 | 不动 | | 添加图片/字体等静态资源 | Metro 自动检测 | 不动 | | `package.json` 加了纯 JS 依赖 | `npm insta

## 10:19

清理完毕。对比一下：  ---  ### `gradle.properties` — 砍掉的 Expo 残留  ```diff - expo.gif.enabled=true - expo.webp.enabled=true - expo.webp.animated=false - EX_DEV_CLIENT_NETWORK_INSPECTOR=true - expo.useLegacyPackaging=false - android.extraMavenRepos=[] ```  ### `app/build.gradle` — 砍掉的 Expo 残留  ```diff - useLega

## 10:21

好的，回顾一下刚才做的事，按时间线：  1. **Expo → React Native CLI 迁移** — 改了入口文件（`index.js` + `src/App.tsx`）、路由（expo-router → react-navigation）、砍了 12 个 expo 依赖、替换了 Babel/Metro/TS 配置、清理了 Gradle 和 AndroidManifest 中的 Expo 元数据  2. **你问要不要 `npx react-native init` 重新初始化** — 我说不需要，当前 `android/` 已经是标准 RN CLI 工程结构  3. **你问 `s

## 11:21

找到了，一抓一大把。给你看全貌：  ---  ## Expo 残留清单  ### MainActivity.kt — 3 处 | 行 | 残留 | 类型 | |----|------|------| | `import expo.modules.ReactActivityDelegateWrapper` | Expo 特有类 | 🔴 | | `ReactActivityDelegateWrapper(...)` | 包装器调用 | 🔴 | | `onCreate(savedInstanceState)` → `onCreate(null)` | Expo splash-screen 做法 |

## 11:33

TypeScript 编译 ✅ 零错误。全项目 `expo` 扫描：**0 处命中**。  ---  ## 变更总览  | 层级 | 文件 | 动作 | |------|------|------| | 🔴 **MainActivity.kt** | 砍掉 `ReactActivityDelegateWrapper`、修正 `onCreate(null)` → `onCreate(savedInstanceState)` | 重写 | | 🔴 **MainApplication.kt** | 砍掉 `ReactNativeHostWrapper`、`ApplicationLifecycleDi

## 11:36

---  ## ✅ 复查结论：零 Expo 残留  逐文件对照 RN CLI 标准模板检查：  ### 入口 & 配置层  | 文件 | 内容 | 判定 | |------|------|------| | `package.json` | scripts 全是 `react-native *`，deps 零 `expo-*` | ✅ | | `index.js` | `AppRegistry.registerComponent` | ✅ | | `app.json` | 仅 `name` + `displayName` | ✅ | | `babel.config.js` | `@react-

## 13:28

完全没有影响。音频采集模块的链路完好无损：  ``` JS 层                          Kotlin 原生层 ───────                       ──────────── AudioCaptureModule.ts  ──→   AudioCaptureModule.kt   NativeModules                   @ReactMethod startCapture()   .AudioCapture                   @ReactMethod stopCapture()          │

## 13:44

这个项目是 Expo SDK 52 + Reanimated 3.16，`android/` 目录在 `prebuild` 后生成（本地才有）。问题分两个层面：  ---  ## 第一步：Windows 系统层面（立即做，5 分钟）  ### A. 启用长路径支持（治本） **管理员 PowerShell** 执行： ```powershell New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `   -Name "LongPathsEnabled" -Value 1 -PropertyType

## 13:48

你说得对，我犯蠢了。这是纯 RN CLI 项目，`android/` 是直接管理的，根本没有 prebuild 这回事。回到正题：  ---  ## 两个层面各解决各的  ### 1. Windows 长路径限制（一次性，5 分钟）  **管理员 PowerShell**： ```powershell New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" `   -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force ``` 重启生效

## 13:49

可以试试，但**大概率不够**。  问题不在 Windows 本身支不支持长路径——启用注册表后 Win32 API 确实放开了 260 限制。但 **CMake + Ninja 在 Windows 上有自己的路径限制**，它们编译 Reanimated C++ 时不走 `\\?\` 长路径 API，仍然卡 260 字符。  不过既然你已经改了注册表，先直接跑一次 Android Studio 编译试试——能过就省事了。  没过的话不用移项目，我直接改 `gradle.properties` 限制只编译 `arm64-v8a`，路径能短一半以上。需要我看看你当前的 `gradle.proper

## 14:18

搞定。两个 `companion object` 已合并为一个，全部 5 个常量都在同一处：  ```kotlin companion object {     const val NAME = "AudioCapture"                    // public — @ReactModule 注解需要     private const val TAG = "AudioCapture"     private const val DEFAULT_SAMPLE_RATE = 16000     private const val DEFAULT_CHANNEL_COUNT =

## 14:48

好，分析完了。问题根因和需要改的东西都清楚了。  ## 诊断结果  **根因**：Android 15 (API 35) 强制要求所有 `.so` 文件的 ELF LOAD segment 按 16KB 对齐。你的项目用了 NDK 26.x（默认 4KB 对齐），而且 `build.ninja` 里的 `LINK_FLAGS` 也没有 `-Wl,-z,max-page-size=16384`。  **能自己编译的**（改了 NDK + 编译参数就能修复）： - `libappmodules.so` - `libreact_codegen_*.so`（safeareacontext、screen

## 14:56

TypeScript 编译通过 ✅。现在来看最终结果：  ---  ## ✅ 升级完成：React Native 0.76.9 → 0.77.1  ### 改了什么  | 包 | 旧版本 | 新版本 | 原因 | |----|--------|--------|------| | `react-native` | 0.76.9 | **0.77.1** | 首个原生支持 16KB ELF 对齐的 RN 版本 | | `expo` | ~52.0.0 | **~52.0.27** | Expo 官方要求的最低版本才能兼容 RN 0.77 | | `react-native-reanimated`

