# FE 工作日志 — 2026-07-10

## 09:04

项目已重建，两个 bug 都修好了。总结：  ---  ## 修复对比  | Bug | 根因 | 修复 | |-----|------|------| | **开始采集 → 白屏** | JS 层未捕获异常 → React 组件树崩溃 | 三层防护：① `screen.tsx` 所有 native 调用 try/catch → `setErrorMsg()` 红色错误横幅 ② `ErrorBoundary` 兜底组件树 ③ Native `start()` 全部异常走 `promise.reject()` | | **授权系统音频 → 闪退** | `onActivityResult` 中

## 09:11

好了，我已经**逐行手 trace 了每一个代码路径**。结果如下：  ---  ## 模拟测试报告：两个 Bug 全路径覆盖  ### Bug#1 白屏 — 10 条路径全部安全  | # | 路径 | 防护层 | 结果 | |---|------|--------|------| | 1 | 初始状态未授权 → 点"开始采集" | UI 层 `canStart=false`，按钮 disabled | ✅ 点不到 | | 2 | 仅授权录音，系统音频未授权 | `canStart` 仍为 false | ✅ 点不到 | | 3 | 两权限都授权 → 正常开始 | try/catch 三层包

## 09:12

好，Kotlin 源码 219 行 + JS Bridge 109 行，逐行过完了。结论先说：  ---  ## 原生代码深度审查结果  **两个 crash bug 确实修好了** — `onActivityResult` 的三层防护（resultCode → data null → try/catch）和 `start()` 的异常全 reject 都是正确的。但逐行 trace 后发现 **5 个新问题**，2 个必须修：  ---  ### 🔴 Must Fix  **#1 — `start()` 异常后状态泄漏**  ```kotlin // 第 147 行附近 audioRecor

## 09:15

全部改完。5 个问题，5 处修改，两边的文件已同步。总结：  ---  ## 修复总结  | # | 问题 | 位置 | 修改 | |---|------|------|------| | **1** | `start()` 异常后状态泄漏 | Kotlin L226 | catch (Exception) 里加 `stopCapture()` 回滚 AudioRecord + isCapturing | | **2** | read 死循环（ERROR_DEAD_OBJECT = -6） | Kotlin L213-216 | 加 `else if (read < 0)` 兜底所有未知错误码

## 16:03

好，Reanimated 装上了，Babel 插件也配好了。来分析下你这两个报错：  ---  ## 错误分析  两个错误其实**同一根因**：`react-native-reanimated` 没装。  | 错误 | 原因 |  |------|------| | `valueUnpacker ENOENT` | Reanimated 的原生库被 `react-native-screens` 间接拉进来了，但 JS 端没装，Metro 找不到它内部的序列化文件 | | `Tried to synchronously call a non-worklet function on the UI

## 16:13

好，分析完了，两个错误的根因和修复如下：  ---  ## 诊断  | # | 错误 | 根因 | |---|------|------| | **1** | `Media projections require a foreground service of type FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION` | Android 14+ 硬性要求：使用 MediaProjection 前必须有一个 `mediaProjection` 类型的前台服务在跑。现在代码里直接拿 `MediaProjection` 对象，没启动服务 → 系统不给用 | | *

## 16:20

改动完成。总结一下两个修复：  ---  ## 改了什么  ### 错误 ①：MediaProjection 前台服务崩溃  **文件**：`AudioCaptureModule.kt`  **根因**：前台服务启动和 `getMediaProjection()` 之间有竞态——服务可能还没跑完 `startForeground()`，系统就检查了 foreground service 状态。  **修复**：把 `startMediaProjectionService()` 从 `OnActivityResult`（授权回调里）**提前到** `requestMediaProjection(

