# 轻松面试助手 — 任务：{任务名称}
状态：待开发

## 工作目录
> ⚠️ 所有开发任务必须在各自的工作目录下进行，禁止跨目录操作。

| 角色 | 工作目录 |
|------|----------|
| FE（小柯） | `workspace/fe/` |
| BE（酱瓜） | `workspace/be/` |
| 共享契约 | `workspace/shared/` |

## 需求描述
{PM 填写：一句话目标 + 用户场景 + 核心功能点}

## API 契约
> ⚠️ 锁定后禁止单方面修改。变更需 FE+BE 重新协商 + 更新此文档 + 通知 PM。

| 方法 | 路径 | 请求体 | 响应体 | 状态码 |
|------|------|--------|--------|--------|
| GET | /api/xxx | — | {items, total} | 200 |
| POST | /api/xxx | {field1, field2} | {data: {id, field1}} | 201 |

## FE 任务清单
> 所有 FE 开发在 `workspace/fe/` 下进行
- [ ] {任务项}
- [ ] {任务项}

## BE 任务清单
> 所有 BE 开发在 `workspace/be/` 下进行
- [ ] {任务项}
- [ ] {任务项}

## 技术决策记录
{FE+BE 协商后填写，包括：通信方式选择、字段命名约定、分页规范、缓存策略等}

## 联调结果
- FE 验证：{姓名} 已验证接口可用 — {日期}
- BE 确认：{姓名} 已确认响应符合契约 — {日期}

## 验收结果
- PM 验收：{通过/不通过}
- 不通过原因：{如适用}
- 验收日期：{日期}
