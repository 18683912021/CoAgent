# 轻松面试助手 — 任务：0.1 项目脚手架搭建
状态：待开发

## 工作目录
> ⚠️ 所有开发任务必须在各自的工作目录下进行，禁止跨目录操作。

| 角色 | 工作目录 |
|------|----------|
| FE（小柯） | `workspace/fe/rn-app-shell/` |
| BE（酱瓜） | `workspace/be/` |
| 共享契约 | `workspace/shared/` |

## 需求描述
搭建项目基础脚手架，确保 FE 和 BE 能独立运行并完成一次端到端通信验证。

- **FE**：确认 Expo RN 项目配置就绪（已有 rn-app-shell），补充项目基础信息（app name/identifier），跑通 `npx expo start`
- **BE**：初始化后端项目，选定技术栈，创建基础目录结构，跑通健康检查接口
- **联调**：FE 调用 BE 健康检查接口，确认端到端连通

## API 契约
> ⚠️ 锁定后禁止单方面修改。变更需 FE+BE 重新协商 + 更新此文档 + 通知 PM。

| 方法 | 路径 | 请求体 | 响应体 | 状态码 |
|------|------|--------|--------|--------|
| GET | /api/health | — | `{"status": "ok", "timestamp": "ISO8601"}` | 200 |

## FE 任务清单
> 所有 FE 开发在 `workspace/fe/rn-app-shell/` 下进行
- [ ] 确认 app.config.ts 中 app name 设为「轻松面试助手」
- [ ] 确认 package.json 依赖完整（expo、expo-av、react-native 等）
- [ ] `npx expo start` 能正常启动
- [ ] 添加一个简单的首页，显示「轻松面试助手」标题
- [ ] 首页添加一个按钮，调用 `GET /api/health` 并显示返回状态

## BE 任务清单
> 所有 BE 开发在 `workspace/be/` 下进行
- [ ] 选定技术栈并初始化项目（建议 Node.js/Express 或 Python/FastAPI，酱瓜自行决定）
- [ ] 创建基础目录结构（src/、tests/、config/）
- [ ] 实现 `GET /api/health` 健康检查接口
- [ ] 配置 CORS，允许本地 FE 开发端口跨域访问
- [ ] 本地启动服务并验证 `/api/health` 返回正确 JSON

## 技术决策记录
{FE+BE 协商后填写}

## 需求变更记录
> FE/BE 对 PRD 的质疑 + PM 的重评估结论，全部记录在此。

| 序号 | 日期 | 提出方 | 质疑内容 | 理由 | 建议方案 | PM 结论 | PM 理由 |
|------|------|--------|---------|------|---------|---------|---------|
| — | — | — | — | — | — | — | — |

## 联调结果
- FE 验证：{姓名} 已验证接口可用 — {日期}
- BE 确认：{姓名} 已确认响应符合契约 — {日期}

## 验收结果
- PM 验收：{通过/不通过}
- 不通过原因：{如适用}
- 验收日期：{日期}

---

## 验收标准
- [ ] FE `npx expo start` 正常启动，首页显示「轻松面试助手」
- [ ] BE 服务正常启动，`GET /api/health` 返回 200 和正确 JSON
- [ ] FE 首页点击按钮，成功调用 BE `/api/health` 并在页面显示「服务正常」字样
- [ ] BE 日志能看到来自 FE 的请求记录
